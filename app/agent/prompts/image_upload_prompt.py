"""
昴云助手 - 图片上传分析提示词模块
负责生成图片入库所需的智能字段提示词
"""


def get_image_upload_analysis_prompt() -> str:
    """
    获取图片上传分析的提示词
    
    Returns:
        用于多模态LLM的提示词字符串
    """
    return """你是云相册智能助手的图片分析专家。请仔细分析这张图片，并生成入库所需的智能字段。

**重要：只输出纯JSON字符串，不要包含任何Markdown标记（如```json）、不要添加解释文字、不要输出其他内容。**

返回格式要求：
{
    "name": "图片名称（简洁明了，不超过20字）",
    "introduction": "详细描述画面内容、氛围、情感等（50-100字）",
    "category": "分类（必须从以下选项中选择其一：风景、二次元、人物、动物、美食、旅游、表情、壁纸、其他）",
    "tags": ["标签1", "标签2", "标签3"]  // 3-5个关键词，描述画面元素、风格、情感等
}

字段规则：
1. name：简洁有吸引力，不超过20字
2. introduction：生动详细，50-100字，描述画面内容、色彩、氛围
3. category：必须从给定选项中选择，不能自创分类
4. tags：具体且有代表性，避免过于宽泛的词汇（如"好看"、"不错"），每个标签不超过10字
5. 如果图片包含人物，描述时保持尊重
6. 如果无法识别某些字段，使用合理的默认值，不要留空
7. **绝对禁止**：不要输出```json标记、不要输出markdown代码块、不要添加解释文字

示例（注意：这只是示例格式，实际输出不要包含这些说明文字）：
{"name":"樱花树下的少女","introduction":"春日暖阳下，粉白色的樱花花瓣随风飘落，一位穿着校服的少女站在樱花树下，脸上洋溢着灿烂的笑容。背景是柔和的蓝天和远处的教学楼，整个画面充满了青春的气息。","category":"二次元","tags":["樱花","少女","春天","校园","动漫"]}"""
